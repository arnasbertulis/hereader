DATABASE_USER=postgres
DATABASE_PASSWORD=dev
JWT_SECRET=changeme
# The deployed .env should carry the deployed origin only — this
# comma-separated dev default is for local docker compose, not production.
CORS_ALLOWED_ORIGINS=http://localhost:*,https://204-168-240-12.sslip.io