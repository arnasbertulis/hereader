name: epub_reader
description: EPUB container parsing and block normalization for hereader.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

dependencies:
  archive: ^4.0.9
  html: ^0.15.6
  xml: ^7.0.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6