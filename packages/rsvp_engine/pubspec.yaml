name: rsvp_engine
description: Tokenization and pacing for hereader's RSVP reading mode.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.12.0

dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  fake_async: ^1.3.3
